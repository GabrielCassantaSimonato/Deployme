<div id="signup-page"> 
    <div class="signup-card">
        <div class="signup-side-info">
            <img src="/img/logo_deployme.png" alt="Deployme" style="height: 200px; filter: brightness(0) invert(1);"
                class="mb-3" onerror="this.style.display='none'">
            <h2 class="fw-800 mb-4" id="info-title">A tua jornada começa agora.</h2>
            <p class="lead opacity-75 mb-5" id="info-subtitle">Acesso a talentos e vagas reais em um clique.</p>

            <div class="d-flex flex-column gap-4">
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-people-fill text-primary fs-4"></i>
                    <span>conexões entre estudantes e recrutadores</span>
                </div>
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-rocket-takeoff-fill text-primary fs-4"></i>
                    <span>busca pela primeira vaga tech</span>
                </div>
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-shield-lock-fill text-primary fs-4"></i>
                    <span>segurança de dados LGPD</span>
                </div>
            </div>

            <div class="mt-auto pt-5 opacity-50 small">Deployme Hub &bull; Ver. 2.6</div>
        </div>

        <div class="signup-form-side">
            <a href="/" class="back-link">
                <i class="bi bi-arrow-left me-2"></i> Voltar à Home
            </a>
            <h3 class="fw-800 mb-4" id="form-heading">Criar Conta</h3>

            <form id="registrationForm" action="/recruiterRegister" method="POST" enctype="multipart/form-data">

                <!-- Dados Pessoais -->
                <div class="form-section-header">Dados Pessoais</div>
                <div class="row g-3 mb-4">

                    <!-- NOME -->
                    <div class="col-md-8">
                        <div class="form-floating">
                            <input type="text" name="nome"
                                class="form-control <?= isset($this->view->erros['nome']) ? 'is-invalid' : '' ?>"
                                value="<?= $this->view->dados['nome'] ?? '' ?>">

                            <?php if (isset($this->view->erros['nome'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['nome'] ?>
                                </div>
                            <?php } ?>

                            <label>Nome Completo</label>
                        </div>
                    </div>

                    <!-- GENERO -->
                    <div class="col-md-4">
                        <div class="form-floating">
                            <select name="genero_id"
                                class="form-select <?= isset($this->view->erros['genero_id']) ? 'is-invalid' : '' ?>">
                                <option value="" disabled selected>Escolha...</option>

                                <?php foreach ($this->view->generos as $g) { ?>
                                    <option value="<?= $g['id'] ?>"
                                        <?= (isset($this->view->dados['genero']) && $this->view->dados['genero'] == $g['id']) ? 'selected' : '' ?>>
                                        <?= $g['genero'] ?>
                                    </option>
                                <?php } ?>
                            </select>

                            <?php if (isset($this->view->erros['genero_id'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['genero_id'] ?>
                                </div>
                            <?php } ?>

                            <label>Gênero</label>
                        </div>
                    </div>

                    <!-- EMAIL -->
                    <div class="col-12">
                        <div class="form-floating">
                            <input type="email" name="email"
                                class="form-control <?= isset($this->view->erros['email']) ? 'is-invalid' : '' ?>"
                                value="<?= $this->view->dados['email'] ?? '' ?>">

                            <?php if (isset($this->view->erros['email'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['email'] ?>
                                </div>
                            <?php } ?>

                            <label>E-mail de Acesso</label>
                        </div>
                    </div>

                </div>

                <!-- Recrutador -->
                <div class="recruiter-field">
                    <div class="form-section-header">Dados Corporativos</div>

                    <div class="row g-3 mb-4">

                        <!-- EMPRESA -->
                        <div class="col-md-7">
                            <div class="form-floating">
                                <input type="text" name="empresa"
                                    class="form-control <?= isset($this->view->erros['empresa']) ? 'is-invalid' : '' ?>"
                                    value="<?= $this->view->dados['empresa'] ?? '' ?>">

                                <?php if (isset($this->view->erros['empresa'])) { ?>
                                    <div class="invalid-feedback">
                                        <?= $this->view->erros['empresa'] ?>
                                    </div>
                                <?php } ?>

                                <label>Nome da Empresa</label>
                            </div>
                        </div>

                        <!-- SENIORIDADE -->
                        <div class="col-md-4">
                        <div class="form-floating">
                            <select name="senioridade_id"
                                class="form-select <?= isset($this->view->erros['senioridade_id']) ? 'is-invalid' : '' ?>">
                                <option value="" disabled selected>Escolha...</option>

                                <?php foreach ($this->view->senioridade as $senioridade) { ?>
                                    <option value="<?= $g['id'] ?>"
                                        <?= (isset($this->view->dados['senioridade']) && $this->view->dados['senioridade'] == $senioridade['id']) ? 'selected' : '' ?>>
                                        <?= $senioridade['senioridade'] ?>
                                    </option>
                                <?php } ?>
                            </select>

                            <?php if (isset($this->view->erros['senioridade_id'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['senioridade_id'] ?>
                                </div>
                            <?php } ?>

                            <label>Senioridade</label>
                        </div>
                    </div>
                    </div>
                </div>

                <!-- Upload -->
                <div class="form-section-header">Uploads e Segurança</div>

                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="upload-card" for="photo-file">
                            <div class="upload-icon"><i class="bi bi-camera"></i></div>
                            <div class="text-start">
                                <div class="small fw-800 text-dark">Foto de Perfil</div>
                                <div class="x-small text-muted">JPG, PNG (Opcional)</div>
                            </div>
                        </label>
                        <input type="file" name="foto_perfil" id="photo-file" class="d-none" accept="image/*">
                    </div>
                </div>

                <!-- SENHA -->
                <div class="row mt-4">
                    <div class="col-12">
                        <div class="form-floating">
                            <input type="password" name="senha"
                                class="form-control <?= isset($this->view->erros['senha']) ? 'is-invalid' : '' ?>">

                            <?php if (isset($this->view->erros['senha'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['senha'] ?>
                                </div>
                            <?php } ?>

                            <label>Defina sua Senha</label>
                        </div>
                    </div>
                </div>

                <!-- Termos -->
                <div class="form-check my-4">
                    <input class="form-check-input" type="checkbox" name="termos" required id="termsCheck">
                    <label class="form-check-label small text-muted" for="termsCheck">
                        Li e concordo com a
                        <a href="/lgpd" class="text-primary fw-bold text-decoration-none">
                            Política de LGPD
                        </a> da Deployme.
                    </label>
                </div>

                <button type="submit" class="btn btn-primary btn-lg w-100 py-3 shadow-lg">
                    Finalizar Cadastro
                </button>

            </form>
        </div>