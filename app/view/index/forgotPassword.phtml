<div class="container">
    <div class="row justify-content-center min-vh-100 align-items-center">
        <div class="col-md-6 col-lg-5">
            <div class="login-premium-container">

                <a href="/login" class="back-link mb-4 text-decoration-none">
                    <i class="bi bi-arrow-left me-2"></i>
                    Voltar ao login
                </a>

                <div class="login-avatar-ring">
                    <i class="bi bi-key"></i>
                </div>

                <h3 class="fw-800 mb-4 text-center">
                    Redefinir senha
                </h3>

                <?php
                // Renderiza um alerta de perigo caso o parâmetro de erro seja retornado na requisição GET
                if (isset($_GET['erro'])) {
                    ?>
                    <div class="alert alert-danger">
                        <?php
                        // Exibe a mensagem de erro devidamente tratada para evitar brechas de XSS
                        ?>
                        <?= htmlspecialchars($_GET['erro']) ?>
                    </div>
                <?php } ?>

                <form action="/resetPassword" method="POST">
                    <div class="form-floating mb-3">
                        <input type="email" class="form-control" name="email" required>
                        <label>E-mail</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="password" class="form-control" name="senha" minlength="8" required>
                        <label>Nova senha</label>
                    </div>

                    <div class="form-floating mb-4">
                        <input type="password" class="form-control" name="confirmar_senha" minlength="8" required>
                        <label>Confirmar senha</label>
                    </div>

                    <button type="submit" class="btn btn-primary btn-lg w-100">
                        Alterar senha
                    </button>
                </form>

            </div>
        </div>
    </div>
</div>