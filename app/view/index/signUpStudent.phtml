<div id="signup-page">
    <div class="signup-card">
        <div class="signup-side-info">
            <img src="/img/logo_deployme.png" alt="Deployme" style="height: 200px; filter: brightness(0) invert(1);"
                class="mb-3" onerror="this.style.display='none'">
            <h2 class="fw-800 mb-4" id="info-title">A tua jornada começa agora.</h2>
            <p class="lead opacity-75 mb-5" id="info-subtitle">Acesso a talentos e vagas reais em um clique.</p>

            <!-- FRASES ATUALIZADAS CONFORME SOLICITADO -->
            <div class="d-flex flex-column gap-4">
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-people-fill text-primary fs-4"></i>
                    <span>conexões entre estudantes e recrutadores</span>
                </div>
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-rocket-takeoff-fill text-primary fs-4"></i>
                    <span>busca pela primeira vaga tech</span>
                </div>
                <div class="d-flex align-items-center gap-3">
                    <i class="bi bi-shield-lock-fill text-primary fs-4"></i>
                    <span>segurança de dados LGPD</span>
                </div>
            </div>

            <div class="mt-auto pt-5 opacity-50 small">Deployme &bull; Ver. 2.6</div>
        </div>

        <div class="signup-form-side">
            <a href="/" class="back-link">
                <i class="bi bi-arrow-left me-2"></i> Voltar à Home
            </a>
            <h3 class="fw-800 mb-4" id="form-heading">Criar Conta</h3>

            <form id="registrationForm" action="/studentRegister" method="POST" enctype="multipart/form-data">
                <!-- Dados Pessoais -->
                <div class="form-section-header">Dados Pessoais</div>
                <div class="row g-3 mb-4">
                    <div class="col-md-8">
                        <div class="form-floating">
                            <input type="text" name="nome" class="form-control 
                                <?= isset($this->view->erros['nome']) ? 'is-invalid' : '' ?>"
                                value="<?= $this->view->dados['nome'] ?? '' ?>">
                            <?php if (isset($this->view->erros['nome'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['nome'] ?>
                                </div>
                            <?php } ?>
                            <label>Nome Completo</label>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-floating">
                            <select class="form-select" name="genero_id" required>
                                <option value="" selected disabled>Escolha...</option>

                                <?php foreach ($this->view->generos as $g) { ?>
                                    <option value="<?= $g['id'] ?>">
                                        <?= $g['genero'] ?>
                                    </option>
                                <?php } ?>

                            </select>
                            <label>Gênero</label>
                        </div>
                    </div>
                    <div class="col-12">
                        <div class="form-floating">
                            <input type="email" name="email" class="form-control 
                                <?= isset($this->view->erros['email']) ? 'is-invalid' : '' ?>"
                                value="<?= $this->view->dados['email'] ?? '' ?>">
                            <?php if (isset($this->view->erros['email'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['email'] ?>
                                </div>
                            <?php } ?>
                            <label>E-mail de Acesso</label>
                        </div>
                    </div>
                </div>

                <!-- CAMPOS DINÂMICOS: ESTUDANTE -->
                <div class="student-field">
                    <div class="form-section-header">Vida Acadêmica</div>

                    <!-- LINHA 1 -->
                    <div class="row g-3 mb-4">

                        <div class="col-md-6">
                            <div class="form-floating">
                                <select class="form-select" name="universidade_id" required>
                                    <option value="" selected disabled>Escolha...</option>
                                    <?php foreach ($this->view->universidades as $u) { ?>
                                        <option value="<?= $u['id'] ?>"><?= $u['nome'] ?></option>
                                    <?php } ?>
                                </select>
                                <label>Universidade</label>
                            </div>
                        </div>

                        <div class="col-md-4">
                            <div class="form-floating">
                                <select class="form-select" name="curso_id" required>
                                    <option value="" selected disabled>Escolha...</option>
                                    <?php foreach ($this->view->cursos as $c) { ?>
                                        <option value="<?= $c['id'] ?>"><?= $c['nome'] ?></option>
                                    <?php } ?>
                                </select>
                                <label>Curso</label>
                            </div>
                        </div>

                        <div class="col-md-2">
                            <div class="form-floating">
                                <select class="form-select" name="semestre_id" required>
                                    <option value="" selected disabled>...</option>
                                    <?php foreach ($this->view->semestres as $s) { ?>
                                        <option value="<?= $s['id'] ?>"><?= $s['semestre'] ?></option>
                                    <?php } ?>
                                </select>
                                <label>Sem.</label>
                            </div>
                        </div>

                    </div>

                    <div class="row mb-4">
                        <div class="col-12">

                            <div class="github-modern-input input-group">
                                <span class="input-group-text">
                                    <i class="bi bi-github"></i>
                                </span>

                                <div class="form-floating flex-grow-1">
                                    <input type="text" id="github-user" class="form-control" name="github"
                                        placeholder="User">
                                    <label>Seu Username do GitHub</label>
                                </div>
                            </div>

                            <div id="github-feedback" class="mt-2 d-none align-items-center p-3 bg-light rounded-4">
                                <img id="github-avatar" src=""
                                    style="width:40px; border-radius:50%; margin-right:15px; border: 2px solid white;">
                                <span id="github-status" class="small fw-800"></span>
                            </div>

                        </div>
                    </div>
                </div>
                <!-- Localização -->
                <div class="form-section-header">Localização</div>
                <div class="row g-3 mb-4">
                    <div class="col-md-4">
                        <div class="form-floating"><input type="text" class="form-control" id="cep"
                                placeholder="00000-000" name="cep" maxlength="9" onkeyup="mascaraCEP(this)"
                                onblur="buscaViaCEP(this.value)"><label>CEP</label>
                        </div>
                    </div>
                    <div class="col-md-8">
                        <div class="form-floating"><input type="text" name="rua" class="form-control" id="rua"
                                placeholder="Rua"><label>Rua
                                / Logradouro</label></div>
                    </div>
                    <div class="col-md-5">
                        <div class="form-floating"><input type="text" name="bairro" class="form-control" id="bairro"
                                placeholder="Bairro"><label>Bairro</label></div>
                    </div>
                    <div class="col-md-5">
                        <div class="form-floating"><input type="text" name="cidade" class="form-control" id="cidade"
                                placeholder="Cidade"><label>Cidade</label></div>
                    </div>
                    <div class="col-md-5">
                        <div class="form-floating"><input type="text" name="complemento" class="form-control"
                                id="complemento" placeholder="Comp."><label>Complemento</label></div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-floating"><input type="text" name="uf" class="form-control" id="uf"
                                placeholder="UF"><label>UF</label></div>
                    </div>
                </div>
                <!-- Ficheiros -->
                <div class="form-section-header">Uploads e Segurança</div>
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="upload-card" for="photo-file">
                            <div class="upload-icon"><i class="bi bi-camera"></i></div>
                            <div class="text-start">
                                <div class="small fw-800 text-dark">Foto de Perfil</div>
                                <div class="x-small text-muted" id="photo-text">JPG, PNG (Opcional)</div>
                            </div>
                        </label>
                        <input type="file" name="foto_perfil" id="photo-file" class="d-none" accept="image/*">
                    </div>
                    <div class="col-md-6 student-field">
                        <label class="upload-card" for="resume-file">
                            <div class="upload-icon"><i class="bi bi-file-earmark-pdf text-danger"></i>
                            </div>
                            <div class="text-start">
                                <div class="small fw-800 text-dark">Currículo PDF</div>
                                <div class="x-small text-muted" id="resume-text">Max 5MB</div>
                            </div>
                        </label>
                        <input type="file" name="curriculo" id="resume-file" class="d-none" accept="application/pdf">
                        <button type="button" id="btnAnalyze" class="btn-ai-analyze w-100 d-none mt-2">
                            <i class="bi bi-stars me-1"></i>
                            Analisar currículo com IA
                        </button>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-12">
                        <div class="form-floating">
                            <input type="password" name="senha" class="form-control 
                                <?= isset($this->view->erros['senha']) ? 'is-invalid' : '' ?>">

                            <?php if (isset($this->view->erros['senha'])) { ?>
                                <div class="invalid-feedback">
                                    <?= $this->view->erros['senha'] ?>
                                </div>
                            <?php } ?>
                            <label>Defina sua Senha</label>
                        </div>
                    </div>
                </div>

                <div class="form-check my-4">
                    <input class="form-check-input" type="checkbox" required id="termsCheck">
                    <label class="form-check-label small text-muted" for="termsCheck">
                        Li e concordo com a <a href="/lgpd" class="text-primary fw-bold text-decoration-none">Política
                            de LGPD</a> da Deployme.
                    </label>
                </div>

                <button type="submit" class="btn btn-primary btn-lg w-100 py-3 shadow-lg">Finalizar
                    Cadastro</button>
            </form>
        </div>

        <!-- MODAL IA -->
        <div class="modal fade" id="aiModal" tabindex="-1">

            <div class="modal-dialog modal-dialog-centered modal-lg">

                <div class="modal-content border-0 shadow-lg rounded-4 overflow-hidden">

                    <!-- HEADER -->
                    <div class="modal-header bg-dark text-white border-0 p-4">

                        <div>
                            <h4 class="fw-bold mb-1">
                                <i class="bi bi-stars me-2 text-warning"></i>
                                Análise Inteligente
                            </h4>

                            <small class="opacity-75">
                                Feedback gerado por IA
                            </small>
                        </div>

                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal">
                        </button>

                    </div>

                    <!-- BODY -->
                    <div class="modal-body p-5">

                        <!-- SCORE -->
                        <div class="text-center mb-5">

                            <div id="aiScore" class="display-1 fw-bold text-primary">
                                0
                            </div>

                            <div class="text-muted">
                                Score do currículo
                            </div>

                        </div>

                        <!-- FEEDBACK -->
                        <div class="mb-4">

                            <h5 class="fw-bold mb-3">
                                Feedback Geral
                            </h5>

                            <p id="aiFeedback" class="text-muted lh-lg">
                            </p>

                        </div>

                        <!-- PONTOS FORTES -->
                        <div class="mb-4">

                            <h5 class="fw-bold mb-3 text-success">
                                Pontos Fortes
                            </h5>

                            <p id="aiStrong" class="text-muted lh-lg">
                            </p>

                        </div>

                        <!-- MELHORIAS -->
                        <div>

                            <h5 class="fw-bold mb-3 text-warning">
                                O que pode melhorar
                            </h5>

                            <p id="aiImprove" class="text-muted lh-lg">
                            </p>

                        </div>

                    </div>

                </div>

            </div>

        </div>