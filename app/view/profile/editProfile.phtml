<?php

$usuario = $this->view->dadosUsuario;
$tipo = $_SESSION['tipo'];
$universidades = $this->view->universidades;
$cursos = $this->view->cursos;
$semestres = $this->view->semestres;
require_once('../app/view/navbar.phtml');

?>

<?php if (isset($_SESSION['success'])) { ?>

    <div class="custom-alert-success">

        <i class="bi bi-check-circle-fill"></i>

        <?= $_SESSION['success'] ?>

    </div>

    <?php unset($_SESSION['success']); ?>

<?php } ?>

<?php if (isset($_SESSION['error'])) { ?>

    <div class="custom-alert-danger">

        <i class="bi bi-exclamation-circle-fill"></i>

        <?= $_SESSION['error'] ?>

    </div>

    <?php unset($_SESSION['error']); ?>

<?php } ?>
<!-- CARD -->
<div class="edit-profile-wrapper">
    <div class="edit-profile-card">

        <!-- HEADER -->
        <div class="edit-profile-header">

            <div class="edit-cover"></div>

            <div class="edit-profile-top">

                <img src="<?= !empty($usuario['foto'])
                    ? '/uploads/fotos/' . $usuario['foto']
                    : '/uploads/fotos/default-user.png' ?>" class="edit-profile-photo">

                <div>

                    <h2>
                        <?= $usuario['nome'] ?>
                    </h2>

                    <p class="text-muted">

                        <?= $usuario['email'] ?>

                    </p>

                </div>

            </div>

        </div>

        <!-- FORM -->
        <form action="/updateProfile" method="POST" enctype="multipart/form-data">

            <div class="edit-profile-body">

                <!-- CONTA -->
                <div class="edit-section">

                    <h4>

                        <i class="bi bi-person-fill"></i>

                        Informações da conta

                    </h4>

                    <div class="row">

                        <!-- NOME -->
                        <div class="col-md-6 mb-3">

                            <label class="form-label">
                                Nome
                            </label>

                            <input type="text" name="nome" class="form-control" value="<?= $usuario['nome'] ?>">

                        </div>

                        <!-- EMAIL -->
                        <div class="col-md-6 mb-3">

                            <label class="form-label">
                                E-mail
                            </label>

                            <input type="email" name="email" class="form-control" value="<?= $usuario['email'] ?>">

                        </div>

                    </div>

                </div>

                <?php if ($usuario['tipo'] == 'estudante') { ?>

                    <!-- GITHUB -->
                    <div class="edit-section">

                        <h4>

                            <i class="bi bi-github"></i>

                            GitHub

                        </h4>

                        <input type="text" name="github" class="form-control" placeholder="https://github.com/seuusuario"
                            value="<?= $usuario['github'] ?? '' ?>">

                    </div>

                    <!-- FORMAÇÃO -->
                    <div class="edit-section">

                        <h4>

                            <i class="bi bi-mortarboard-fill"></i>

                            Formação acadêmica

                        </h4>

                        <div class="row">

                            <!-- FACULDADE -->
                            <div class="col-md-6 mb-3">

                                <label class="form-label">
                                    Faculdade
                                </label>

                                <select name="universidade_id" class="form-select">

                                    <?php foreach ($universidades as $universidade) { ?>

                                        <option value="<?= $universidade['id'] ?>" <?= isset($usuario['universidade_id'])
                                              && $usuario['universidade_id'] == $universidade['id']
                                              ? 'selected'
                                              : '' ?>>

                                            <?= $universidade['nome'] ?>

                                        </option>

                                    <?php } ?>

                                </select>

                            </div>

                            <!-- CURSO -->
                            <div class="col-md-6 mb-3">

                                <label class="form-label">
                                    Curso
                                </label>

                                <select name="curso_id" class="form-select">

                                    <?php foreach ($cursos as $curso) { ?>

                                        <option value="<?= $curso['id'] ?>" <?= isset($usuario['curso_id'])
                                              && $usuario['curso_id'] == $curso['id']
                                              ? 'selected'
                                              : '' ?>>

                                            <?= $curso['nome'] ?>

                                        </option>

                                    <?php } ?>

                                </select>

                            </div>

                            <!-- SEMESTRE -->
                            <div class="col-md-4 mb-3">

                                <label class="form-label">
                                    Semestre
                                </label>

                                <select name="semestre_id" class="form-select">

                                    <?php foreach ($semestres as $semestre) { ?>

                                        <option value="<?= $semestre['id'] ?>" <?= isset($usuario['semestre_id'])
                                              && $usuario['semestre_id'] == $semestre['id']
                                              ? 'selected'
                                              : '' ?>>

                                            <?= $semestre['semestre'] ?>

                                        </option>

                                    <?php } ?>

                                </select>

                            </div>

                        </div>

                    </div>

                    <!-- ENDEREÇO -->
                    <div class="edit-section">

                        <h4>

                            <i class="bi bi-geo-alt-fill"></i>

                            Endereço

                        </h4>

                        <div class="row g-3">

                            <!-- CEP -->
                            <div class="col-md-4">

                                <div class="form-floating">

                                    <input type="text" class="form-control" id="cep" name="cep" placeholder="00000-000"
                                        maxlength="9" onkeyup="mascaraCEP(this)" onblur="buscaViaCEP(this.value)"
                                        value="<?= $usuario['cep'] ?>">

                                    <label>
                                        CEP
                                    </label>

                                </div>

                            </div>

                            <!-- RUA -->
                            <div class="col-md-8">

                                <div class="form-floating">

                                    <input type="text" name="rua" class="form-control" id="rua" placeholder="Rua"
                                        value="<?= $usuario['rua'] ?>">

                                    <label>
                                        Rua / Logradouro
                                    </label>

                                </div>

                            </div>

                            <!-- BAIRRO -->
                            <div class="col-md-5">

                                <div class="form-floating">

                                    <input type="text" name="bairro" class="form-control" id="bairro" placeholder="Bairro"
                                        value="<?= $usuario['bairro'] ?>">

                                    <label>
                                        Bairro
                                    </label>

                                </div>

                            </div>

                            <!-- CIDADE -->
                            <div class="col-md-5">

                                <div class="form-floating">

                                    <input type="text" name="cidade" class="form-control" id="cidade" placeholder="Cidade"
                                        value="<?= $usuario['cidade'] ?>">

                                    <label>
                                        Cidade
                                    </label>

                                </div>

                            </div>

                            <!-- COMPLEMENTO -->
                            <div class="col-md-5">

                                <div class="form-floating">

                                    <input type="text" name="complemento" class="form-control" id="complemento"
                                        placeholder="Complemento" value="<?= $usuario['complemento'] ?>">

                                    <label>
                                        Complemento
                                    </label>

                                </div>

                            </div>

                            <!-- UF -->
                            <div class="col-md-2">

                                <div class="form-floating">

                                    <input type="text" name="uf" class="form-control" id="uf" placeholder="UF"
                                        value="<?= $usuario['uf'] ?>">

                                    <label>
                                        UF
                                    </label>

                                </div>

                            </div>

                        </div>

                    </div>

                    <!-- CURRÍCULO -->
                    <div class="edit-section">

                        <h4>

                            <i class="bi bi-file-earmark-pdf-fill"></i>

                            Currículo

                        </h4>

                        <?php if (!empty($usuario['curriculo'])) { ?>

                            <div class="current-file">

                                <i class="bi bi-file-earmark-pdf"></i>

                                <?= $usuario['curriculo'] ?>

                            </div>

                        <?php } else { ?>

                            <div class="current-file empty-file">

                                Nenhum currículo cadastrado

                            </div>

                        <?php } ?>

                        <input type="file" name="curriculo" class="form-control mt-3">

                    </div>

                <?php } ?>

                <?php if ($usuario['tipo'] == 'recrutador') { ?>

                    <!-- EMPRESA -->
                    <div class="edit-section">

                        <h4>

                            <i class="bi bi-buildings-fill"></i>

                            Empresa

                        </h4>

                        <input type="text" name="empresa" class="form-control" value="<?= $usuario['empresa'] ?>">

                    </div>

                <?php } ?>

                <!-- FOTO -->
                <div class="edit-section">

                    <h4>

                        <i class="bi bi-camera-fill"></i>

                        Foto de perfil

                    </h4>

                    <div class="current-photo-container">

                        <img src="<?= !empty($usuario['foto'])
                            ? '/uploads/fotos/' . $usuario['foto']
                            : '/uploads/fotos/default-user.png' ?>" class="current-profile-preview">

                        <div>

                            <strong>
                                Foto atual do perfil
                            </strong>

                            <p class="text-muted mb-0">

                                Você poderá alterar sua foto futuramente.

                            </p>

                        </div>

                    </div>

                    <input type="file" name="foto" class="form-control mt-3">

                </div>

                <!-- BOTÃO -->
                <div class="text-end mt-4">

                    <button type="submit" class="save-profile-btn">

                        <i class="bi bi-check-circle-fill"></i>

                        Salvar alterações

                    </button>

                </div>

            </div>

        </form>